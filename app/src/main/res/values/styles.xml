<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="theme_customer_progress_dialog" parent="@android:style/Theme.Material.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:paddingLeft">10dp</item>  //图片与左边距的距离
        <item name="android:paddingRight">10dp</item> //message与右边距的距离
        <item name="android:textSize">20sp</item>      //message字体的大小
        <item name="android:textColor">#ffffff</item>  //message字体的颜色
    </style>

    <style name="ProgressDialog" parent="@android:style/Theme.Holo.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:paddingLeft">10dp</item>  //图片与左边距的距离
        <item name="android:paddingRight">10dp</item> //message与右边距的距离
        <item name="android:textSize">20sp</item>      //message字体的大小
        <item name="android:textColor">#ff000000</item>  //message字体的颜色
    </style>
</resources>
